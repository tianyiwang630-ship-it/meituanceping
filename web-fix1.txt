【角色与背景】
你是一个资深的前端开发工程师。我现在有一个 H5 网页项目，目前在 UI 交互和数据逻辑上存在 3 个问题需要你修复。请根据以下具体需求，修改相关代码（包括 HTML/VUE/REACT、CSS 以及 1.json 数据文件）：

【具体修复任务】

任务 1：实现 PC 端标签栏的鼠标拖拽滑动

问题：包含“赶时间”、“吃土期”等标签的分类栏目前超出了宽度，但在 PC 端无法通过鼠标按住拖动来查看右侧隐藏的标签。

修改要求：

请使用 JavaScript 为该标签栏容器添加鼠标拖拽事件（监听 mousedown、mousemove、mouseup 和 mouseleave），实现鼠标按住即可左右拖拽滑动的效果。

监听 wheel 事件，让鼠标上下滚动也能转化为容器的左右滑动。

使用 CSS 隐藏该容器的默认横向滚动条（::-webkit-scrollbar { display: none; }），保持页面清爽。

任务 2：重构“狠狠安利”与“避雷快逃”的投票互斥逻辑

问题：目前的点赞（安利）和踩（避雷）按钮缺乏状态管理，可以被无限点击或逻辑混乱。

修改要求：请为这两个按钮引入状态管理（例如 voteStatus: 'none' | 'up' | 'down'），并实现以下严格的互斥交互逻辑：

初始状态：未点击，安利和避雷按钮均为默认样式。

点【安利】：当前状态变为 'up'，安利数 +1，按钮变为高亮/选中态。

取消【安利】：在已点【安利】的情况下再次点击【安利】，状态变回 'none'，安利数 -1（取消投票）。

状态切换（从安利切避雷）：在已点【安利】的状态下点击【避雷】，状态变为 'down'，此时安利数 -1，且避雷数 +1，避雷按钮变为高亮态。

反之亦然（避雷切安利同理）。

注意：请确保视图层（数字和按钮高亮状态）能够实时响应数据的变化。